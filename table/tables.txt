
SQLite local database storage
-----------------------------

Documentation from http://www.sqlite.org/

create table bh1750fvi(
no integer primary key,
ts timestamp default current_timestamp,
name varchar(20),
illuminance real
);

create table bme680(
no integer primary key,
ts timestamp default current_timestamp,
name varchar(20),
temperature real,
humidity real,
pressure real,
resistance real,
gasvalid integer,
stable integer
);

create table bmp280(
no integer primary key,
ts timestamp default current_timestamp,
name varchar(20),
temperature real,
pressure real
);

create table lis3dh(
no integer primary key,
ts timestamp default current_timestamp,
name varchar(20),
gxmin real,
gx real,
gxmax real,
gymin real,
gy real,
gymax real,
gzmin real,
gz real,
gzmax real,
adc1 real,
adc2 real,
adc3 real,
odr integer 
);

create table lis2mdl(
no integer primary key,
ts timestamp default current_timestamp,
name varchar(20),
Bx real,
By real,
Bz real,
temperature real
);

create table lis3mdl(
no integer primary key,
ts timestamp default current_timestamp,
name varchar(20),
Bx real,
By real,
Bz real,
temperature real
);

create table tmp102(
no integer primary key,
ts timestamp default current_timestamp,
name varchar(20),
temperature real
);


